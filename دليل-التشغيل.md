# استبيان يعمر — دليل التشغيل

## الملفات

| الملف | الوصف |
|---|---|
| `index.html` | الموقع كامل — ملف واحد بلا أي تبعيات خارجية (الشعار مدمج داخله) |
| `apps-script.gs` | سكربت الحفظ في Google Sheet |
| `sync-json.sh` | سحب الاستجابات من الخادم وكتابتها JSON في مجلد `yamur-backup` بجانب `index.html` |
| `استبيان-يعمر-1.html` | النسخة التجريبية السابقة (للمرجع فقط) |

---

---

## ✅ قائمة ما قبل النشر

| # | ما يجب عمله | الحالة |
|---|---|---|
| 1 | **ارفع `index.html` وحده فقط** — لا ترفع `apps-script.gs` ولا `sync-json.sh`، ففيهما `SECRET` و`ADMIN_KEY` وسيصبحان قابلين للتنزيل من أي أحد | ⚠️ حرج |
| 2 | الصق `apps-script.gs` الجديد ثم `Deploy → Manage deployments → Edit → New version` — بدونها يبقى الرابط على النسخة القديمة بلا نسخ احتياطية | ⚠️ مطلوب |
| 3 | غيّر `ADMIN_KEY` في `apps-script.gs` **و** في `sync-json.sh` (كلاهما لا يزال بالقيمة الافتراضية) | ⚠️ مطلوب |
| 4 | اضبط `BACKUP_EMAIL` لتفعيل نسخة البريد (الطبقة ④) | مستحسن |
| 5 | شغّل `testBackup` مرة من محرر Apps Script لإعطاء صلاحيات Drive والبريد مقدماً | مطلوب |
| 6 | احذف أي صفوف تجريبية من الجدول: قائمة «يعمر» → «عرض قائمة الاستجابات» ثم «حذف استجابة برقمها المرجعي» | مطلوب |
| 7 | أعد زوم المتصفح إلى **100%** — التكبير صار داخل التصميم | تنبيه |
| 8 | لا يوجد رمز دخول الآن: أي شخص لديه الرابط يستطيع الإرسال. تأكد أن هذا هو المطلوب | قرار |

**اختبار حقيقي قبل التوزيع:** عبّئ استبياناً كاملاً بنفسك، تأكد أن الصف ظهر في الجدول
وأن ملف JSON ظهر في مجلد `yamur-backup`، ثم احذفه بقائمة «يعمر».

---

## أجهزة مشتركة بين عدة جمعيات

الإجابات تُحفظ في متصفح الجهاز، فلو استُخدم جهاز واحد لأكثر من جمعية:

- بعد الإرسال الناجح يظهر في شاشة «تم» زر **«تعبئة استبيان لجمعية أخرى على هذا الجهاز»** — يمسح الجهاز ويبدأ استبياناً فارغاً برقم مرجعي جديد.
- وفي شاشة المراجعة زر **«مسح الإجابات والبدء من جديد»** للإلغاء قبل الإرسال.
- الاستجابة المُرسلة تبقى محفوظة في سجل الاتحاد، فالمسح من الجهاز آمن.

> يُفضّل رغم ذلك أن تعبّئ كل جمعية من جهازها، لأن الحفظ التلقائي والاستئناف مرتبطان بالمتصفح.

---

## الربط بجدول البيانات — 6 خطوات

1. أنشئ **Google Sheet** جديد → `Extensions` → `Apps Script`
2. امسح المحتوى الافتراضي والصق ملف `apps-script.gs` كاملاً
3. في السكربت، غيّر `SECRET` إلى كلمة سرية من اختيارك
4. `Deploy` → `New deployment` → Type: **Web app**
   - Execute as: **Me**
   - Who has access: **Anyone**   ← مهم، وإلا لن يستطيع الموقع الحفظ
5. انسخ رابط النشر المنتهي بـ `/exec`
6. في `index.html` عدّل أول قيمتين في `CONFIG`:

```js
const CONFIG = {
  SCRIPT_URL: 'https://script.google.com/macros/s/XXXX/exec',  // رابط النشر
  SECRET: 'نفس-الكلمة-السرية-في-السكربت',                      // مطابقة حرفياً
  ...
```

**للتأكد من الجاهزية:** افتح رابط `/exec` في المتصفح — يجب أن يظهر:
`{"ok":true,"service":"yamur-survey",...}`

> عند أي تعديل لاحق على السكربت: `Deploy` → `Manage deployments` → `Edit` → Version: **New version** → `Deploy`، وإلا يبقى الرابط على النسخة القديمة.

---

## بنية الجدول

صف واحد لكل استجابة، **73 عموداً**:

```
وقت الإرسال │ الرقم المرجعي │ اسم الجمعية │ البريد │ الجوال │ ثم كتلة كل بند │ ثم 6 أعمدة موردين
```

وكل بند كتلة بالترتيب:
`منجز 2023` · `منجز 2024` · `منجز 2025` · `المواصفات المعتمدة` · `تكلفة 2023` · `تكلفة 2024` · `تكلفة 2025` · `مستهدف 2026`
(بند الأنظمة الصوتية يتفرّع إلى ثلاثة مكوّنات، فأعمدة الكمية والتكلفة والمستهدف فيه ثلاثة أضعاف.)

ثم في **آخر الجدول** عمود لكل بند: `<اسم البند> - أبرز الموردين والمقاولين`.
وُضعت في النهاية لا داخل كتلة كل بند لأن سكربت الشيت يُلحق الأعمدة الجديدة في آخر صف العناوين فقط؛ فلو أُقحمت في المنتصف لانزاحت أعمدة الاستجابات المحفوظة سابقاً عن عناوينها.

الكميات والتكاليف تُحفظ **أرقاماً حقيقية** (قابلة للجمع والتحليل مباشرة)، والجوال نصاً للحفاظ على الصفر الأول.

---

## النسخ الاحتياطية — 4 طبقات مستقلة

كل استجابة تُحفظ في 4 أماكن، **فقدان أي واحد لا يفقد البيانات**:

| # | المكان | الملف |
|---|---|---|
| ① | صف في Google Sheet | ورقة «الاستجابات» — للعرض والتحليل |
| ② | ملف JSON مستقل لكل استجابة | `yamur-backup/YM-XXXXXXXX.json` في Drive |
| ③ | ملف JSON جامع لكل الاستجابات | `yamur-backup/_all-responses.json` |
| ④ | نسخة بالبريد مع مرفق JSON | خارج Drive تماماً — اضبط `BACKUP_EMAIL` لتفعيلها |

الطبقات **مستقلة فعلياً**: لو سقط Drive بالكامل تبقى نسخة البريد، ولو تعطّل البريد يبقى الملفان،
ولو ضاعت كل النسخ يبقى الجدول — ومنه تُعاد بناء النسخ كلها بضغطة واحدة.

**الجدول هو مصدر الاستعادة:** قائمة «يعمر» → «إعادة بناء النسخة من الجدول» تعيد إنشاء
كل ملفات JSON من صفوف الجدول، مفيدة لو كانت النسخ معطّلة فترة.

### قائمة «يعمر» داخل الجدول
تظهر بعد إعادة تحميل الجدول:

- **تصدير نسخة JSON الآن** — يحدّث النسخ ويرسلها بالبريد إن كان مُهيَّأً
- **إعادة بناء النسخة من الجدول** — يستعيد كل ملفات JSON من الصفوف
- **فتح مجلد النسخ الاحتياطية** — يطبع رابط المجلد
- **عرض قائمة الاستجابات** — الرقم المرجعي والجمعية والوقت لكل صف
- **حذف استجابة برقمها المرجعي** — يحذفها من الجدول **ومن كل النسخ** معاً

### تصدير كل البيانات برابط

```
https://.../exec?action=export&key=ADMIN_KEY
```

`ADMIN_KEY` موجود في `apps-script.gs` فقط و**لا يوضع في index.html أبداً** — اجعله طويلاً وعشوائياً.

> **تنبيه أمني:** `SECRET` مكتوب داخل `index.html`، فأي شخص يفتح كود الصفحة يراه.
> وظيفته منع الإرسال العابر فقط، **وليس حماية البيانات**. لذلك التصدير محمي بمفتاح مختلف.

### نسخة محلية بجانب index.html

```bash
./sync-json.sh            # سحب من الخادم
./sync-json.sh --from تصدير.json   # من ملف نزّلته يدوياً
```

يكتب في مجلد `yamur-backup` **داخل نفس مجلد `index.html`**:
ملف `YM-XXXXXXXX.json` لكل استجابة + `_all-responses.json` جامعاً.

- عدّل `ADMIN_KEY` في أعلى `sync-json.sh` مرة واحدة — نفس المفتاح في `apps-script.gs`.
- يكتب فقط ما اختلف فعلاً، ويطبع كم ملفاً جديداً وكم محدَّثاً وكم بلا تغيير.
- لا يحذف شيئاً — الملفات القديمة تبقى حتى لو حُذفت الاستجابة من الجدول.
- شغّله وقت ما تشاء؛ ولو أردته تلقائياً أضفه إلى `cron` أو `launchd`.

> **لماذا سكربت ولا حفظ مباشر:** `index.html` صفحة ثابتة، والمتصفح لا يملك صلاحية الكتابة
> في مجلده، وApps Script يكتب في Google Drive لا في قرصك. السحب اليدوي هو الطريق الوحيد
> بلا Backend حقيقي.

### استرجاع طارئ من جهاز الجمعية
لو تعذّر وصول استجابة إلى الخادم نهائياً، إجاباتها تبقى في متصفح الجمعية.
افتح Console في متصفحها واكتب `downloadBackup()` فينزل ملف JSON كامل.
**لا يوجد زر لهذا في الواجهة — المستخدم لا يراه إطلاقاً.**

---

## ضمانات الحفظ

| الحالة | ما يحدث |
|---|---|
| نجاح الحفظ | الخادم يقرأ الصف بعد كتابته ويتأكد أنه كُتب فعلاً، ثم يرد بالنجاح. رسالة النجاح لا تظهر إلا بعد هذا التأكيد |
| انقطاع الشبكة | 3 محاولات تلقائية بفواصل متزايدة، ثم رسالة خطأ واضحة + زر إعادة المحاولة. البيانات تبقى على الجهاز |
| ضغط «إرسال» مرتين | الرقم المرجعي يمنع تكرار الصف — الخادم يرد بالنجاح دون إضافة صف ثانٍ |
| إرسال جمعيتين في نفس اللحظة | `LockService` يمنع تضارب الكتابة |
| فشل النسخ الاحتياطية | الجدول محفوظ والرد ناجح، والتحذير يُسجَّل في Console — لا يُوقف العملية |
| بيانات ناقصة | الإرسال يتوقف قبل الشبكة، والرسالة تسمّي البند الناقص بالاسم |
| الرابط غير مُهيَّأ | رسالة خطأ صريحة — **لا رسالة نجاح كاذبة أبداً** |

---

## التحقق من المدخلات

**الحقول الرقمية** — تُنقّى أثناء الكتابة نفسها:
الأرقام العربية `٢٥٠٠` → `2500` · فواصل الآلاف `1,200` → `1200` · المسافات تُحذف · الإشارات والرموز والنصوص والصيغ (`=SUM`) مرفوضة · حد 12 خانة وخانتان عشريتان.

**المواصفات (نص حر)** — حد 400 حرف مع عدّاد ظاهر، وحذف محارف التحكّم ومحارف قلب اتجاه النص.

**الموردون والمقاولون (نص حر)** — حد 200 حرف، حقل مطلوب مثل المواصفات: حرفان على الأقل، ويُقبل «لا يوجد».

**بيانات الجمعية** — الاسم 3 أحرف على الأقل · البريد بصيغة صحيحة · الجوال `05xxxxxxxx` مع تحويل `+966` و`00966` والأرقام العربية تلقائياً.

**حماية الجدول** — أي خلية تبدأ بـ `=` أو `+` أو `-` أو `@` تُحيَّد بعلامة `'` على الواجهة **وفي الخادم** معاً، فلا تُنفَّذ كصيغة في Excel.

---

## الحفظ والرجوع

- كل إدخال يُحفظ تلقائياً في `localStorage` بعد 0.4 ثانية، ويظهر «✓ محفوظ» أعلى الصفحة.
- حفظ فوري إضافي عند مغادرة الصفحة أو تصغيرها.
- عند العودة: يُستأنف الاستبيان **من نفس السؤال** بكل الإجابات، مع رسالة «تم استعادة إجاباتك».
- بعد الإرسال الناجح تبقى شاشة «تم» مع الرقم المرجعي حتى لو أُعيد تحميل الصفحة.
- زر «مسح الإجابات والبدء من جديد» في شاشة المراجعة (مع تأكيد).

---

## النشر

`index.html` ملف واحد مستقل — ارفعه على أي استضافة ثابتة
(Netlify Drop · GitHub Pages · Cloudflare Pages · أو أي سيرفر).


---

## الألوان

ألوان هوية يعمر: الأخضر `#005B36` والزيتوني `#9DB35A`، والشعار بألوانه الأصلية.
في الوضع الليلي يُستخدم أخضر أفتح `#3FBF87` للنصوص الصغيرة ليبقى مقروءاً على الأسود،
والشعار يُضاء ليظهر بلونه الأخضر لا أبيض وأسود.
