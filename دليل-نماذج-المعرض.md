# نماذج المعرض التطوعي — دليل التشغيل

نموذجان إلكترونيان لمشاركة **جمعية يعمر للعناية بالمساجد** في المعرض التطوعي
بجامعة الملك فهد للبترول والمعادن. لكل نموذج رابط ورمز QR مستقل، والاثنان
يكتبان في **جدول بيانات واحد بورقتين منفصلتين**.

| النموذج | الملف | الورقة في الجدول | بادئة الرقم المرجعي |
|---|---|---|---|
| الفرص التطوعية | `volunteer.html` | الفرص التطوعية | `YV-` |
| مشاريع التخرج | `capstone.html` | مشاريع التخرج | `YP-` |

الروابط بعد النشر على GitHub Pages:

- https://yaamur-get.github.io/survey/volunteer.html
- https://yaamur-get.github.io/survey/capstone.html

رموز QR جاهزة في مجلد `qr/` (PNG للشاشة والطباعة العادية، SVG للطباعة الكبيرة)،
وصفحة `qr.html` تعرض الرمزين في بطاقتين جاهزتين للطباعة.

---

## ١) الربط بجدول البيانات — مرة واحدة

**الخطوات:**

1. أنشئ Google Sheet جديداً باسم مثل «نماذج المعرض التطوعي — يعمر».
2. من الجدول: **Extensions ← Apps Script**.
3. امسح المحتوى الافتراضي، والصق ملف `apps-script-kfupm.gs` كاملاً.
4. عدّل في أعلى الملف:
   - `SECRET` — أي نص، بشرط أن يطابق حقل `secret` في الملفين `volunteer.html`
     و `capstone.html` **تماماً**. (القيمة الحالية في الثلاثة: `YAAMUR-KFUPM-2026`.)
   - `ADMIN_KEY` — نص طويل وعشوائي، لا يوضع في أي ملف HTML.
5. **Deploy ← New deployment ← Type: Web app**
   - `Execute as: Me`
   - `Who has access: Anyone` ← مهم جداً، وإلا فشل الإرسال.
6. انسخ رابط النشر المنتهي بـ `/exec`.
7. الصق الرابط في **الملفين** داخل الحقل `scriptUrl`:

```js
scriptUrl: 'https://script.google.com/macros/s/AKfy.../exec',
```

> الرابط نفسه للنموذجين — الخادم يوجّه كل نموذج إلى ورقته عبر الحقل `form`.

8. للتأكّد: افتح رابط `/exec` في المتصفح مباشرة. يجب أن يظهر:

```json
{"ok":true,"service":"yaamur-kfupm-forms","forms":{"volunteer":0,"capstone":0},...}
```

9. جرّب إرسال استجابة واحدة من كل نموذج قبل المعرض، وتأكّد من ظهور الصف
   في الورقة الصحيحة.

**تنبيه:** عند أي تعديل لاحق على ملف Apps Script لا بدّ من
**Deploy ← Manage deployments ← Edit ← Version: New version ← Deploy**،
وإلا بقي الرابط يشغّل النسخة القديمة.

---

## ٢) ما الذي يُحفظ ولماذا لن تُفقد البيانات

كل استجابة تُكتب في ثلاثة أماكن مستقلة:

1. **صف في ورقة النموذج** — وهو مصدر العرض والتحليل.
2. **ملف JSON مستقل** لكل استجابة في مجلد `yaamur-kfupm-backup` في Drive.
3. **ملف JSON جامع لكل نموذج** (`_volunteer.json` و `_capstone.json`) في المجلد نفسه.

الخادم لا يردّ بالنجاح إلا بعد التأكّد فعلياً من كتابة الصف. وإن فشلت النسخ
الاحتياطية فالبيانات محفوظة في الورقة على كل حال.

على جهاز الطالب: إجاباته تُحفظ أولاً بأول في `localStorage`، فلو أُغلقت
الصفحة أو انقطع الاتصال عاد وأكمل من حيث توقّف. وإن فشل الإرسال ثلاث مرات
تبقى الإجابات على الجهاز وتظهر رسالة تدعوه للمحاولة مرة أخرى.

---

## ٣) الاستخدام في المعرض

- الأجهزة المشتركة: بعد كل إرسال يظهر زر **«تعبئة نموذج جديد»** يمسح إجابات
  الطالب السابق ويبدأ استجابة جديدة برقم مرجعي مستقل.
- كل استجابة تحمل رقماً مرجعياً يظهر للطالب في شاشة الإتمام — استخدمه عند
  أي استفسار لاحق.
- النماذج تعمل على الجوال والحاسب، وتدعم المظهر الفاتح والداكن (زر ◑ في الأعلى).
- الأرقام العربية (٠١٢٣) تُحوَّل تلقائياً إلى لاتينية، ورقم الجوال يُقبل
  بصيغ `05…` و `9665…` و `009665…` ويُخزَّن موحّداً بصيغة `05xxxxxxxx`.

---

## ٤) الحقول

### الفرص التطوعية (١٣ حقلاً + وقت الإرسال والرقم المرجعي)

الاسم الثلاثي · رقم الجوال · البريد الإلكتروني · التخصّص · السنة الدراسية ·
المجال التطوعي (اختيار متعدّد، مع حقل «أخرى») · المهارات · نوع التطوّع المفضّل ·
هل سبق لك التطوّع · أين تطوّعت سابقاً (يظهر عند «نعم») · ملاحظات إضافية ·
الموافقة على التواصل.

### مشاريع التخرج (١٧ حقلاً + وقت الإرسال والرقم المرجعي)

اسم الطالب · رقم الجوال · البريد الإلكتروني · التخصّص · عدد أعضاء الفريق ·
تخصّصات الأعضاء · هل لديكم فكرة · عنوان الفكرة ووصفها (يظهران عند «نعم») ·
مجال المشروع (مع حقل «أخرى») · المرحلة الحالية · ما تحتاجونه من الجمعية
(اختيار متعدّد، مع حقل «أخرى») · هل ترغبون في تحدٍّ مقترح · ملاحظات إضافية ·
الموافقة على التواصل.

**نصّ الموافقة في النموذجين:**

> أوافق على استخدام بيانات التواصل للتواصل معي حول الفرص والمشاريع، ومشاركة
> المبادرات والبرامج والأخبار ذات العلاقة بالجمعية.

الموافقة إلزامية — لا يمكن الإرسال بدونها، وتُسجَّل في عمود مستقل.

---

## ٥) تعديل الحقول لاحقاً

الحقول مُعرَّفة في أعلى كل ملف HTML داخل `window.YFORM`، ولا حاجة لفتح
`assets/engine.js` إطلاقاً. لإضافة حقل، أضف كائناً إلى `fields` في الخطوة
المناسبة:

```js
{ k:'mftah', t:'اسم الحقل', type:'text', max:120, ph:'مثال…' }
```

الأنواع المتاحة: `text` · `tel` · `email` · `number` · `select` · `radio` ·
`checks` (اختيار متعدّد) · `textarea` · `consent`.

خيارات مفيدة:

| الخيار | الأثر |
|---|---|
| `req:false` | الحقل اختياري (تظهر كلمة «اختياري» بجانب التسمية) |
| `opts:[...]` | خيارات `select` و `radio` و `checks` |
| `other:true` | يضيف حقل نصّ يظهر عند اختيار «أخرى» |
| `showIf:function(D){...}` | يُظهر الحقل بشرط — مثل `D.hasIdea === 'نعم'` |
| `vald:'name'\|'email'\|'tel'` | تحقّق خاص بدل التحقّق العام |
| `col:'اسم العمود'` | اسم العمود في الجدول إن أردته مختلفاً عن التسمية |
| `hint:'…'` | سطر توضيحي تحت التسمية |
| `list:[...]` | اقتراحات تظهر أثناء الكتابة في حقل نصّي |

> عمود الجدول يأخذ اسمه من `col` أو من `t`. **تغيير اسم حقل موجود يضيف عموداً
> جديداً** بدل الكتابة في القديم — لذا إن عدّلت التسمية بعد بدء جمع البيانات
> فثبّت `col` على الاسم القديم.

**بعد أي تعديل:** غيّر `storeKey` (مثلاً `yaamur_volunteer_v2`) حتى لا تُستأنف
على الأجهزة إجاباتٌ قديمة بحقول لم تعد موجودة.

---

## ٦) رموز QR

الرموز الحالية مولَّدة لروابط GitHub Pages أعلاه، بمستوى تصحيح خطأ عالٍ (H)
مع شعار يعمر في الوسط، وقد جُرّبت قراءتها حتى حجم ٢٦٠ بكسل مع تشويش.

**إن تغيّر النطاق** فأعد توليدها بنفس الإعدادات — أي مولّد QR يفي بالغرض،
بشرط: مستوى تصحيح الخطأ **H**، وهامش أبيض لا يقلّ عن ٤ وحدات، ولون داكن
`#08704C` على خلفية بيضاء.

---

## ٧) استخراج البيانات

- **من الجدول مباشرة:** File ← Download ← CSV / Excel.
- **من قائمة «يعمر — نماذج المعرض»** داخل الجدول:
  عدد الاستجابات · إعادة بناء النسخ الاحتياطية · فتح مجلد النسخ · حذف استجابة برقمها.
- **تصدير JSON عن بُعد:**
  `‎<رابط /exec>?action=export&form=volunteer&key=<ADMIN_KEY>`

**استرجاع طارئ من جهاز طالب** (إن تعذّر وصول استجابته للخادم ولم تُغلق الصفحة):
افتح Console في متصفحه واكتب `downloadBackup()` — يُنزَّل ملف JSON بكل إجاباته.

---

## ٨) ملفات المشروع

```
volunteer.html            نموذج الفرص التطوعية (المخطَّط فقط)
capstone.html             نموذج مشاريع التخرج (المخطَّط فقط)
qr.html                   بطاقتا الرابط والرمز — جاهزتان للطباعة
apps-script-kfupm.gs      خادم Google Apps Script للنموذجين
assets/engine.js          محرّك النماذج المشترك — لا يُعدَّل لإضافة حقل
assets/yaamur.css         أنماط هوية يعمر
assets/yaamur-logo*.png   الشعار (فاتح وداكن) مستخرجاً من دليل الهوية
assets/pattern.svg        نمط الضفيرة — علامة مائية
qr/*.png, qr/*.svg        رموز QR
```

**عن الخط:** النماذج تستخدم الخط الرسمي **DG Shamael** المعتمد في مطبوعات يعمر،
محوَّلاً إلى `assets/DGShamael-Regular.woff2` (‎١٦ كيلوبايت) مع نسخة `.woff`
للمتصفّحات القديمة، معرَّفاً في أعلى `assets/yaamur.css`.

المتوفّر من الخط وزن **Regular** فقط، فالعناوين والتسميات العريضة يُعرّضها
المتصفّح تركيبياً. وإن وصلتكم لاحقاً أوزان Bold أو Black من المورّد، أضيفوا
لكل وزن كتلة `@font-face` بنفس اسم العائلة و `font-weight` المناسب — ويلتقطها
التصميم تلقائياً دون أي تعديل آخر.

خط `Almarai` يبقى احتياطاً في سلسلة الخطوط لأمرين: محارف قليلة خارج تغطية
DG Shamael (‎« » — · ✓ ◑) تُرسم به، وأي جهاز يفشل فيه تحميل الخط يعود إليه.

**تنبيه ترخيصي:** DG Shamael خط تجاري. نشره ضمن الموقع يجعل ملفّه قابلاً
للتحميل من أي زائر. بتّ القرار بنشره — ويبقى التحقّق من أن رخصتكم تغطّي
الاستخدام على الويب (Webfont) مسؤوليةً على الجمعية.
